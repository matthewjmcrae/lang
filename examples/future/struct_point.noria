// Future example: struct parameters and pass-by-value are deferred to Phase 5 wake 3.
// Field assignment (p.x = v) is deferred to Phase 5 wake 2.

struct Point {
  x: i32;
  y: i32;
}

fn abs(value: i32) -> i32 {
  if value < 0 {
    return -value;
  } else {
    return value;
  }
}

fn manhattan(point: Point) -> i32 {
  return abs(point.x) + abs(point.y);
}

fn main() -> i32 {
  let origin: Point = Point { x: -3, y: 4 };
  return manhattan(origin);
}
