import std::sequence::{Sequence, sequence_new};

fn main() -> i32 {
  let numbers: Sequence<i32> = sequence_new(0);
  let flags: Sequence<bool> = sequence_new(false);
  let sum = numbers + flags;
  return 0;
}
