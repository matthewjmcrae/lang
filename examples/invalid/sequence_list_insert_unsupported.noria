import std::sequence::{Sequence, sequence_insert, sequence_new, sequence_push};

fn main() -> i32 {
  let s: Sequence<i32, list> = sequence_new(0);
  s = sequence_push(s, 10);
  s = sequence_insert(s, 0, 5);
  return 0;
}
