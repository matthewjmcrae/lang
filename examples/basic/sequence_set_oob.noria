import std::sequence::{Sequence, sequence_new, sequence_push, sequence_set};

fn main() -> i32 {
  let s: Sequence<i32, arr> = sequence_new(0);
  sequence_push(s, 10);
  sequence_set(s, 5, 1);
  return 0;
}
