struct Point {
  x: i32;
  y: i32;
}

struct Rect {
  origin: Point;
}

fn main() -> i32 {
  let r: Rect = Rect { origin: Point { x: 1, y: 2 } };
  r.origin.x = 3;
  return r.origin.x + r.origin.y;
}
