fn fib(n: i32) -> i32 {
  if n <= 1 {
    return n;
  } else {
    return fib(n - 1) + fib(n - 2);
  }
}

fn main() -> i32 {
  let value: i32 = fib(6);
  let matches: bool = value == 8;

  if matches {
    return value + 1;
  } else {
    return 0;
  }
}
