struct Point {
  x: i32;
  y: i32;
}

fn main() -> i32 {
  let p: Point = Point { x: 3, y: 4 };
  p.x = 10;
  p.y = 20;
  p.x = p.x + 1;
  let i: i32 = 0;
  while i < 3 {
    p.y = p.y + 1;
    i = i + 1;
  }
  return p.x + p.y;
}
