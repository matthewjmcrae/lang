fn factorial(n: i32) -> i32 {
  if n <= 1 {
    return 1;
  } else {
    return n * factorial(n - 1);
  }
}

fn main() -> i32 {
  let i: i32 = 1;
  let total: i32 = 0;

  while i <= 4 {
    total = total + factorial(i);
    i = i + 1;
  }

  return total;
}
