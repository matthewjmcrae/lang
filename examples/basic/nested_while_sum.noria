fn main() -> i32 {
  let outer: i32 = 0;
  let total: i32 = 0;

  while outer < 3 {
    let inner: i32 = 0;
    while inner < 4 {
      total = total + outer + inner;
      inner = inner + 1;
    }
    outer = outer + 1;
  }

  return total;
}
