struct Holder {
  value: str;
}

fn replace(h: Holder) -> void {
  h.value = "new";
  return;
}

fn main() -> i32 {
  let h: Holder = Holder { value: "old" + " value" };
  replace(h);
  if h.value == "old value" {
    return 0;
  }
  return 1;
}
